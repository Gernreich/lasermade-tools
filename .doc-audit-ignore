# boxes.py is the external generator at https://www.festi.info/boxes.py/ — an
# author and a tool, never a file here.

boxes.py
RegularBox.svg

# Placeholders in the usage lines. They are what you type in place of, not
# files kept here.

OUT.svg
FILE.svg
CUTFILE.svg
PAGE.html
WRITEUP.md
prose.py

# Named in prose as examples of tools that know one build, and so live in the
# repository they describe rather than in this one.

bell.py
verify_torus.js
BuildA1_90_25.svg

# In the build repositories these check, not here.
bore_split.py
BuzzDisc1.svg

# doc-audit.py's write-up of the ".json read as .js" wrong answer names the phantom it
# used to invent and the missing file its bite test still catches. Examples, not files.

reduced.js
ghost.js

# The living-hinge figure whose two descriptions had drifted. Named in prose to
# say where the drift is; both files live in that repository, not this one.

living-hinge-guide.md
panel-convention.svg

# all-gates.sh runs these two from trumpet/tools; they are named to say what it
# covers, and they live in the repository they gate.

check.py
regress.py

# The page that does NOT exist, which is the whole point of the sentence naming
# it: pairing README.md with README.html is what made the script silently skip
# three repositories.

README.html

# The Boxes.py generator all-gates.sh compares against its installed copy. It
# lives in trumpet/tools and is installed into the Boxes checkout; neither is
# here.

snakeboxvar.py
